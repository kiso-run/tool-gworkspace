[project]
name = "tool-gworkspace"
version = "0.2.0"
description = "Google Workspace tool for Kiso — wraps the gws CLI"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
managed = true

[tool.pytest.ini_options]
pythonpath = ["."]
